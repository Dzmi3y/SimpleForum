@using SimpleForumApp.ViewModels
@model LoginModel
@{
    ViewData["Title"] = "Авторизация";
}

<form asp-controller="Account" asp-action="Login" asp-antiforgery="true">
    <div class="validation" asp-validation-summary="ModelOnly"> </div>
    <div>
        <label asp-for="Email">Введите Email </label>
        <input type="text" asp-for="Email" class="input-group-append" />
        <span asp-validation-for="Email" />
    </div>
    <div>
        <label asp-for="Password">Введите пароль </label>
        <input type="password" asp-for="Password" class="input-group-append" />
        <span asp-validation-for="Password" />
    </div>
    <br />
    <div>
        <input type="submit" class="btn btn-secondary" value="Войти" />
    </div>
</form>